const backtrack = require("../src/backtrack/backtrack");
const assert = require("assert");

describe("backtrack", function () {
  xit("should test", function () {
    let results = Array();
    backtrack(3, results);
    //assert.equal(result.size, 2);
  });
});
